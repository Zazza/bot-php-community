-- 000009_drop_topics.up.sql — удаление библиотеки тем для обсуждения (фича убрана:
-- синтетические LLM-темы раздражали). Пакет topics оставлен ради еженедельного дайджеста.
-- topic_digests НЕ трогается. Идемпотентно (IF EXISTS).

DROP TABLE IF EXISTS topics;
